// src/app/layout.jsx
import "./globals.css";
import { CartProvider } from "./context/CartContext";

export const metadata = {
  title: "EcoStride",
  description: "Sustainable sneakers made from recycled and eco-friendly materials",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <CartProvider>{children}</CartProvider>
      </body>
    </html>
  );
}
